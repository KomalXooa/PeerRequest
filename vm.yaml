imports:
- path: vm_template.jinja

resources:
- name: ExternalVm
  type: vm_template.jinja
  properties:
    zone: us-central1-a
    startup-script: |
      #!/bin/bash
      sudo wget https://raw.githubusercontent.com/KomalXooa/PeerRequest/master/peersetup.sh
      sudo chmod a+x /peersetup.sh
      
